«Резервное копирование баз данных» - Чернов Николай

Задание 1. Резервное копирование
Кейс
Финансовая компания решила увеличить надёжность работы баз данных и их резервного копирования.

Необходимо описать, какие варианты резервного копирования подходят в случаях:

1.1. Необходимо восстанавливать данные в полном объёме за предыдущий день.
     полный ежедневный бэкап.
     
1.2. Необходимо восстанавливать данные за час до предполагаемой поломки.
     полный + инкрементный/дифференциальный бэкап + бинарные логи → PITR.
     
1.3.* Возможен ли кейс, когда при поломке базы происходило моментальное переключение на работающую или починенную базу данных.
      репликация/кластер + автоматическое переключение (Failover).

Задание 2. PostgreSQL
2.1. С помощью официальной документации приведите пример команды резервирования данных и восстановления БД (pgdump/pgrestore).

pg_dump -U username -d database_name > backup.sql

psql -U username -d database_name < backup.sql

2.1.* Возможно ли автоматизировать этот процесс? Если да, то как?

crontab -e 

0 2 * * * pg_dump -U username -d database_name -F c -f /backups/$(date +\%F).dump

Задание 3. MySQL
3.1. С помощью официальной документации приведите пример команды инкрементного резервного копирования базы данных MySQL.

mysqlbackup --defaults-file=/home/dbadmin/my.cnf \
--incremental=optimistic \
--incremental-base=history:last_backup \
--backup-dir=/home/dbadmin/temp_dir \
--backup-image=incremental_image1.bi backup-to-image

3.1.* В каких случаях использование реплики будет давать преимущество по сравнению с обычным резервным копированием?

Репликация даёт преимущество в случаях, когда требуется:

Высокая доступность данных. При отказе основного сервера реплики могут мгновенно взять на себя его функции, минимизируя время простоя. 

Распределение нагрузки. Реплики позволяют направлять операции чтения на них, снижая нагрузку на основной сервер. 

Географическое распределение. Реплики в разных регионах сокращают задержки для пользователей в разных частях мира. 

Быстрое восстановление после сбоев. Переключение на реплику занимает 1–3 минуты, тогда как восстановление из резервной копии может занять дни. 
Создание тестовых сред. Реплики позволяют тестировать изменения без риска для производственных данных. .
